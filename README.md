# stable-matching

the algorithm that matches residents to hospitals and earned shapley a nobel prize. one side proposes, the other holds or rejects, nobody ends up in a blocking pair.

the deep result — the proposer-optimal matching is simultaneously the receiver-pessimal matching — is the most game-theoretic sentence ever written.
